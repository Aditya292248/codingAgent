{
  "name": "graph-core",
  "version": "0.1.0",
  "private": true,
  "description": "Local parsing, indexing, and graph queries",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": ["lib"],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "shared": "workspace:*"
  }
}
